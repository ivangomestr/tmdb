import styled from 'styled-components'

export const Container = styled.div`
   background: var(--purple-light);
   display: flex;
   align-items: center;
   height: 5vh;

   img {
       width: 7rem;
       height: 7rem;
       margin-left: 4%;
   }
`